# Sample repository

読み取り専用の`reviewer` Worker例です。`worker.yaml`の `openai/model-name`は、利用環境でPiから使用できるモデル名へ置き換えてください。

Pixariumをnpmからglobal installし、このディレクトリをGitリポジトリとして初期化した後、次のように確認できます。

```bash
git init
pixarium check reviewer
pixarium list
```
